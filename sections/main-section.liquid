<section
  class="grid items-center py-[calc(var(--spacing)*0.75)] lg:py-[--spacing]"
  style="--spacing: {{ section.settings.spacing }}px">
  <div class="container mx-auto px-4 flex flex-col items-start gap-4 md:gap-5">
    {% render 'image', class: 'h-[50vh] lg:h-[30rem]', image: section.settings.image %}
    <h1 class="max-w-xl scroll-m-20 text-4xl font-medium tracking-tight lg:text-5xl">
      {{ section.settings.heading }}
    </h1>
    <p class="max-w-prose text-xl text-gray-500">
      {{ section.settings.text }}
    </p>
    {% if section.settings.button_label != blank %}
      <a
        {% if section.settings.button_link != blank %}
          href="{{ section.settings.button_link }}"
        {% else %}
          href="{{ routes.root_url }}"
        {% endif %}
        class="inline-flex items-center justify-center px-6 h-11 font-medium rounded-sm text-white bg-black">
        {{ section.settings.button_label }}
      </a>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Main Section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Welcome to theme starter template by Odestry"
    },
    {
      "type": "text",
      "id": "text",
      "label": "Text",
      "default": "Start building your theme by editing this section file."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Get Started"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "range",
      "id": "spacing",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Vertical Spacing",
      "default": 48
    }
  ],
  "presets": [
    {
      "name": "Main Section"
    }
  ]
}
{% endschema %}
